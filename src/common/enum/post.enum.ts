export enum Allow_Comment_Enum {
    allow = "allow",
    deny = "deny"
}

export enum Availability_Enum {
    public = "public",
    only_me = "only_me",
    friends = "friends"
}

export enum Likes_Enum {
    disLike = "disLike",
    Like = "Like"
}